name: CI

on:
  push:
    branches: ['**']
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests:
    name: Tests (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    env:
      QT_QPA_PLATFORM: offscreen
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: requirements-test.txt
      - name: Install Linux image and Qt dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends mtools libegl1 libopengl0 libpulse0
      - name: Install Windows image tools
        if: runner.os == 'Windows'
        id: msys2
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          install: mingw-w64-ucrt-x86_64-mtools
      - name: Expose Windows image tools to Python
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          MSYS2_LOCATION: ${{ steps.msys2.outputs.msys2-location }}
        run: |
          $mtoolsBin = Join-Path $env:MSYS2_LOCATION 'ucrt64\bin'
          if (-not (Test-Path -LiteralPath $mtoolsBin -PathType Container)) {
            throw "MSYS2 UCRT64 binary directory was not found: $mtoolsBin"
          }
          Add-Content -Path $env:GITHUB_PATH -Value $mtoolsBin -Encoding utf8
      - name: Install test dependencies
        run: python -m pip install -r requirements-test.txt
      - name: Install Linux HFE test dependency
        if: runner.os == 'Linux'
        # Greaseweazle v1.23, pinned to its upstream source commit.
        run: python -m pip install git+https://github.com/keirf/greaseweazle@a0ae343d7e2603b9f3fdc0149ef8e89de5399f58
      - name: Install Windows HFE test dependency
        if: runner.os == 'Windows'
        shell: pwsh
        run: ./scripts/stage_windows_hfe_tool.ps1 -Destination "$env:RUNNER_TEMP/aps-hfe"
      - name: Verify image tools are available
        run: python scripts/check_mtools.py
      - name: Verify HFE helper is available
        run: gw convert --help
      - name: Check Python syntax
        run: python -m compileall -q aps_midi_prep_tool.py aps_midi_prep_tool_app scripts
      - name: Run automated tests
        run: python -m pytest -q
      - name: Check whitespace
        run: git diff --check
      - name: Check AppImage build script
        if: runner.os == 'Linux'
        run: bash -n scripts/build_appimage.sh
